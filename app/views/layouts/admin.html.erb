<!DOCTYPE html>
<html class="h-full bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50">
  <head>
    <title>Admin Console - <%= content_for(:title) || "API Gateway" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%# Chartkick for charts %>
    <script src="https://www.gstatic.com/charts/loader.js"></script>
    <%= javascript_include_tag "chartkick", "data-turbo-track": "reload" %>

    <%# Action Cable for WebSockets %>
    <%= javascript_include_tag "actioncable", "data-turbo-track": "reload" %>

    <style>
      /* Beautiful modern admin styling with vibrant colors */
      body.admin-layout {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        background: linear-gradient(135deg, #EBF4FF 0%, #E0E7FF 50%, #EDE9FE 100%);
      }

      /* Smooth glassmorphism effect for cards */
      .glass-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.5);
      }

      /* Animated gradient backgrounds */
      @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      .animated-gradient {
        background: linear-gradient(270deg, #6366f1, #8b5cf6, #ec4899, #6366f1);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
      }

      /* Glowing effect for status indicators */
      @keyframes glow {
        0%, 100% { box-shadow: 0 0 5px rgba(34, 197, 94, 0.5), 0 0 10px rgba(34, 197, 94, 0.3); }
        50% { box-shadow: 0 0 10px rgba(34, 197, 94, 0.8), 0 0 20px rgba(34, 197, 94, 0.5); }
      }

      .status-glow {
        animation: glow 2s ease-in-out infinite;
      }

      /* Critical alert flash (RED border) */
      @keyframes criticalFlash {
        0%, 100% { border-color: transparent; }
        50% { border-color: #DC2626; }
      }

      .critical-flash {
        animation: criticalFlash 2s ease-in-out;
        border: 4px solid transparent;
      }

      /* Force white backgrounds on all form inputs */
      input[type="text"],
      input[type="email"],
      input[type="password"],
      input[type="number"],
      input[type="date"],
      textarea,
      select {
        background-color: white !important;
        color: #1f2937 !important;
      }

      /* Fix autofill colors */
      input:-webkit-autofill,
      input:-webkit-autofill:hover,
      input:-webkit-autofill:focus,
      input:-webkit-autofill:active {
        -webkit-box-shadow: 0 0 0 30px white inset !important;
        -webkit-text-fill-color: #1f2937 !important;
      }

      /* Countdown timer highlighting */
      .countdown-timer.text-red-600 {
        font-weight: bold;
        animation: pulse 1s infinite;
      }

      @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.7; }
      }

      /* Smooth hover transitions */
      .smooth-hover {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .smooth-hover:hover {
        transform: translateY(-2px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }
    </style>
  </head>

  <body class="h-full admin-layout">
    <div class="flex h-full">
      <%# Fixed Sidebar %>
      <%= render 'admin/shared/sidebar' %>

      <%# Main Content Area %>
      <main class="flex-1 overflow-y-auto">
        <%# Top Bar - Stunning Gradient Header %>
        <div class="animated-gradient sticky top-0 z-10 shadow-2xl">
          <div class="px-8 py-6">
            <div class="flex items-center justify-between">
              <%# Left Side - Title & Status %>
              <div class="flex items-center space-x-6">
                <%# Page Title with Icon %>
                <div class="flex items-center space-x-3">
                  <div class="bg-white/20 backdrop-blur-sm rounded-2xl p-3">
                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                    </svg>
                  </div>
                  <div>
                    <h1 class="text-3xl font-bold text-white tracking-tight drop-shadow-lg">
                      <%= content_for(:page_title) || 'Admin Console' %>
                    </h1>
                    <p class="text-sm text-white/80 mt-1 font-medium">
                      Gateway Control Center
                    </p>
                  </div>
                </div>

                <%# Connection Status Indicators - Modern Glass Style %>
                <div class="flex items-center space-x-3 bg-white/10 backdrop-blur-md rounded-2xl px-5 py-3 border border-white/20 shadow-lg">
                  <div class="flex items-center space-x-2" id="redis-status-indicator">
                    <div class="w-3 h-3 rounded-full bg-green-400 status-glow"></div>
                    <span class="text-sm font-semibold text-white">Redis</span>
                  </div>
                  <div class="w-px h-6 bg-white/20"></div>
                  <div class="flex items-center space-x-2" id="db-status-indicator">
                    <div class="w-3 h-3 rounded-full bg-green-400 status-glow"></div>
                    <span class="text-sm font-semibold text-white">Database</span>
                  </div>
                </div>
              </div>

              <%# Right Side - User Info & Actions %>
              <div class="flex items-center space-x-4">
                <%# WebSocket Status with Glass Effect %>
                <div id="ws-connection-status" class="px-4 py-2 bg-white/10 backdrop-blur-md rounded-xl border border-white/20 shadow-lg">
                  <span class="text-white/70 text-sm font-medium">● Connecting...</span>
                </div>

                <%# User Info Badge - Glass Morphism %>
                <div class="flex items-center space-x-3 bg-white/15 backdrop-blur-md rounded-2xl px-5 py-3 border border-white/25 shadow-xl">
                  <div class="flex items-center justify-center w-10 h-10 rounded-full bg-gradient-to-br from-yellow-400 to-pink-500 text-white font-bold text-lg shadow-lg">
                    <%= current_user.email[0].upcase %>
                  </div>
                  <div class="hidden md:block">
                    <p class="text-sm font-bold text-white">
                      <%= current_user.email.split('@').first.titleize %>
                    </p>
                    <p class="text-xs text-white/70 font-medium">
                      <%= current_user.role.titleize %>
                    </p>
                  </div>
                </div>

                <%# Logout Button - Modern Style %>
                <%= link_to logout_path, method: :delete, data: { turbo_method: :delete },
                    class: 'flex items-center space-x-2 px-5 py-3 bg-white/20 hover:bg-white/30 backdrop-blur-md text-white rounded-xl transition-all duration-300 shadow-lg border border-white/30 smooth-hover' do %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                  </svg>
                  <span class="font-semibold text-sm">Logout</span>
                <% end %>
              </div>
            </div>
          </div>
        </div>

        <%# Flash Messages with Modern Design %>
        <% if flash.any? %>
          <div class="px-8 py-6">
            <% flash.each do |type, message| %>
              <div class="glass-card <%= type == 'notice' ? 'border-l-4 border-green-500' : 'border-l-4 border-red-500' %> rounded-xl p-4 mb-3 shadow-lg smooth-hover">
                <div class="flex items-center">
                  <div class="<%= type == 'notice' ? 'bg-green-100' : 'bg-red-100' %> rounded-lg p-2 mr-3">
                    <svg xmlns="http://www.w3.org/2000/svg" class="<%= type == 'notice' ? 'text-green-600' : 'text-red-600' %> h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <% if type == 'notice' %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                      <% else %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                      <% end %>
                    </svg>
                  </div>
                  <span class="<%= type == 'notice' ? 'text-green-800' : 'text-red-800' %> font-medium"><%= message %></span>
                </div>
              </div>
            <% end %>
          </div>
        <% end %>

        <%# Page Content %>
        <div class="px-8 py-6">
          <%= yield %>
        </div>
      </main>
    </div>

    <%# Real-time Alert Notifications Container %>
    <div id="admin-alerts" class="fixed top-20 right-6 z-50 max-w-md space-y-2"></div>

    <%# Include admin real-time JavaScript %>
    <%= javascript_include_tag "admin_realtime", "data-turbo-track": "reload" %>

    <%# Admin WebSocket Connection Script %>
    <script>
      // Simple connection status checker (no fancy WebSocket for now)
      function checkConnectionStatus() {
        fetch('/health/detailed', {
          method: 'GET',
          headers: {
            'Accept': 'application/json'
          }
        })
          .then(res => {
            if (!res.ok) {
              throw new Error('Health check failed with status ' + res.status);
            }
            return res.json();
          })
          .then(data => {
            console.log('Health check response:', data);

            // Update Redis indicator
            const redisContainer = document.getElementById('redis-status-indicator');
            const redisIndicator = redisContainer?.querySelector('div');
            if (redisIndicator) {
              if (data.checks?.redis?.status === 'up') {
                redisIndicator.className = 'w-3 h-3 rounded-full bg-green-400 status-glow';
              } else {
                redisIndicator.className = 'w-3 h-3 rounded-full bg-red-500 animate-pulse';
              }
            }

            // Update DB indicator
            const dbContainer = document.getElementById('db-status-indicator');
            const dbIndicator = dbContainer?.querySelector('div');
            if (dbIndicator) {
              if (data.checks?.database?.status === 'up') {
                dbIndicator.className = 'w-3 h-3 rounded-full bg-green-400 status-glow';
              } else {
                dbIndicator.className = 'w-3 h-3 rounded-full bg-red-500 animate-pulse';
              }
            }

            // Update WebSocket status
            const wsStatus = document.getElementById('ws-connection-status')?.querySelector('span');
            if (wsStatus) {
              wsStatus.className = 'text-white text-sm font-semibold';
              wsStatus.textContent = '● Connected';
            }
          })
          .catch(err => {
            console.error('Health check failed:', err);

            // Show disconnected status
            const wsStatus = document.getElementById('ws-connection-status')?.querySelector('span');
            if (wsStatus) {
              wsStatus.className = 'text-red-300 text-sm font-semibold';
              wsStatus.textContent = '● Disconnected';
            }
          });
      }

      // Check every 10 seconds
      setInterval(checkConnectionStatus, 10000);

      // Check on page load
      document.addEventListener('DOMContentLoaded', checkConnectionStatus);

      // Check immediately on Turbo navigation
      document.addEventListener('turbo:load', checkConnectionStatus);
    </script>
  </body>
</html>
